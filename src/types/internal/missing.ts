export const enum NoPackage {
  no_package = "<no_package>",
}
export type NoPackageValues = `${NoPackage}`;

export const enum NoFlag {
  no_flag = "<no_flag>",
}
export type NoFlagValues = `${NoFlag}`;
